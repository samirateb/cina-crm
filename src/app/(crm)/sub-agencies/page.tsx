'use client';
import { useState } from 'react';
import { Heading } from '@/components/ui';
import { Toolbar,DataTable } from '@/components/data-table';
import { AgencyCreate } from '@/components/agency-create';
import { Building2,Download,Plus } from 'lucide-react';
import { useCrmRole } from '@/components/shell';
export default function Agencies(){const [create,setCreate]=useState(false);const {role}=useCrmRole();const isAdmin=role==='super_admin';return <><Heading icon={Building2} title="Sub Agency" gradient="Management" subtitle="Manage your downstream sub-agencies and partners." action={isAdmin?<button onClick={()=>setCreate(true)} className="gradient-bg flex items-center gap-2 rounded-xl px-4 py-2.5 text-xs font-bold text-white"><Plus size={15}/> New Sub Agency</button>:undefined}/><div className="mb-6 grid gap-3 sm:grid-cols-2">{isAdmin&&<button onClick={()=>window.dispatchEvent(new Event('cina:table-export'))} className="surface flex items-center gap-3 rounded-2xl p-4 text-left hover:border-emerald-300"><span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-50 text-emerald-600"><Download size={18}/></span><div className="font-bold">Export<div className="mt-0.5 text-[9px] font-bold tracking-[.12em] text-slate-400">CURRENT FILTER RESULTS · CSV</div></div></button>}{isAdmin&&<button onClick={()=>setCreate(true)} className="surface flex items-center gap-3 rounded-2xl p-4 text-left hover:border-indigo-300"><span className="gradient-bg grid h-10 w-10 place-items-center rounded-xl text-white"><Plus size={18}/></span><div className="font-bold">New<div className="mt-0.5 text-[9px] font-bold tracking-[.12em] text-slate-400">SUB AGENCY</div></div></button>}</div><Toolbar placeholder="Search sub agencies..."/><DataTable type="agencies"/>{create&&<AgencyCreate close={()=>setCreate(false)}/>}</>}
